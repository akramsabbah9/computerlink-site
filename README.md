# computerlink-site

## Installation

1) install dependencies
2) create new postgres database (need to add sql file to do it from terminal)
3) run ```psql mydatabase < node_modules/connect-pg-simple/table.sql``` to add the session table
